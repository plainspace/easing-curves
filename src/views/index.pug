---
layout: base
title: Easing
---
.toast
  span Copied to Clipboard

header
  div 
    h1 Easing
    code v1.0
  div
    span made by <a href="https://johnmeguerian.com">jmegs</a>
    span source on <a href="https://github.com/jmegs/easing-curves">github</a>


for val in easings
  section.easing(id=`${val.name}`)
    //- refactor this to be a semantic header
    div.easing__header
      h2= val.name
      input.sr-only.js_clipboard_text(value=`${val.css}`) 
    div.dot(style=`transition-timing-function: ${val.css}; animation-timing-function: ${val.css}`)
    div.easing__footer 
      span.easing__help-text #{val.css}
      span.easing__help-text Click to Copy
